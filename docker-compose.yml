services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: for_her
      MYSQL_USER: couple_user
      MYSQL_PASSWORD: change_me
      MYSQL_ROOT_PASSWORD: change_root_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data:
